Окружение на poetry. Соответственно в описании шаг для poetry.
В БД должна быть создана база данных с именем apr_test. И предполагается что
Elasticsearch тоже установлен и нет каких то специфических настроек безопасности.

В БД создаем таблицу для постов:
    create table posts(
        id serial,
        rubrics varchar(255),
        text text,
        created_date timestamptz);

- Клонируем репозиторий
- в корневой папке ставим зависимости и активируем окружение
    poetry install && poetry shell
- запускаем питонячий скрипт (читаем csv и пишем в БД)
    python write_in_db.py
